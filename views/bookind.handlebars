<div id="book-page">
  <a href="/books">Voltar</a>
  <div class="container">
    <h1>{{bookind.title}}</h1>
    <p>Este livro tem {{bookind.pageqty}} páginas.</p>
    <div class="actions">
      <a href="/books/edit/{{bookind.id}}">Editar</a>
      <form action="/books/remove/{{bookind.id}}" method="post">
        <input type="submit" value="Remover">
      </form>
    </div>
  </div>
</div>